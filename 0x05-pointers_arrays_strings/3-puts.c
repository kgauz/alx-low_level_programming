#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _puts - print a string
 * followed by a new line
 * @str: receive string value from main function
 */
void _puts(char *str)
{
	puts(str);
}
