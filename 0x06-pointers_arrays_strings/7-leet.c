#include "main.h"
/**
 * char *leet - encodes a string into 1337
 * @n: contains string
 * Return: string
 */
char *leet(char *n)
{
	int i;

	for (i = 0; n[i] != '\0'; i++)
	{
		if (n[i] == a || n[i] == A || n[i] =)
		{
			for (; n[i] = 
