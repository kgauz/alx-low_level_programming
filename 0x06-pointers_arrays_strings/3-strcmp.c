#include "main.h"
#include <string.h>
/**
 * _strcmp - compares two strings
 * @s1: compare it with s2
 * @s2: compare it with s1
 * Return: int
 */
int _strcmp(char *s1, char *s2)
{
	int comp;

	comp = strcmp(s1, s2);
	return (comp);
}
