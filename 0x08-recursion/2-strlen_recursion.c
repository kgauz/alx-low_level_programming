#include "main.h"
#include <string.h>
/**
 * _strlen_recursion - return length of a string
 * @s: string
 * Return: length
 */
int _strlen_recursion(char *s)
{
	int len = strlen(s);

	return (len);
}
